<article>
  <h1>Story Title: <%= @story.story_title %></h1>
  <p><%= @story.story %></p>
</article>

<% if !is_admin? %>
  <%= button_to "Your Stories", author_stories_path(current_author), method: :get %>
  <%= button_to "Edit", edit_author_story_path(@story), method: :get %>
<% end %>
